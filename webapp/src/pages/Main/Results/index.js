import React from 'react';
import PropTypes from 'prop-types';

import {Container} from './styles';

function Results({isbn}) {
  return <div></div>;
}

Results.propTypes = {isbn: PropTypes.string.isRequired};

export default Results;
